import { ThrottleClickDirective } from './throttle-click.directive';

describe('TrottleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ThrottleClickDirective();
    expect(directive).toBeTruthy();
  });
});
